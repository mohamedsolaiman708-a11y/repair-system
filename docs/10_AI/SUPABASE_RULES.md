# Supabase Rules

Use UUID.

Enable Row Level Security.

Never disable RLS.

Use Foreign Keys.

Use Database Functions when appropriate.

Use Realtime only when necessary.

Never expose Service Role Key.

Store secrets securely.

Use Storage Buckets for images.

Use Edge Functions for WhatsApp automation if backend logic grows.