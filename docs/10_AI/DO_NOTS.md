Never put business logic inside widgets.

Never access Supabase from UI.

Never skip repository layer.

Never use dynamic.

Never use print().

Use logger instead.

Never create duplicated widgets.

Never hardcode colors.

Never hardcode strings.

Use localization-ready strings.

Never ignore exceptions.

Always use Result/Failure pattern.

Never exceed 300 lines per file unless justified.